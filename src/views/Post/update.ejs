<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body>

    <div class="text-center">
        <h1 style=" color: rgb(255, 153, 0);">Sửa sản phẩm</h1>
        <form action="" method="post" class="form-control w-75 m-lg-auto bg-secondary">


            <% if(objDT !=null && obj!=null){%>

                <div class="row">
                    <div class="col-md-6">
                        <div class="input-group flex-nowrap mb-1">
                            <span class="input-group-text" id="addon-wrapping">Title</span>
                            <input type="text" class="form-control" placeholder="Tên sản phẩm" aria-label="NameProduct"
                                aria-describedby="addon-wrapping" name="NameProduct" value="<%= obj.title %>">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="input-group flex-nowrap mb-1">
                            <span class="input-group-text" id="addon-wrapping">Content</span>
                            <input type="number" class="form-control" placeholder="Giá tiền" aria-label="Price"
                                aria-describedby="addon-wrapping" name="Price" value="<%= obj.content %>">
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="input-group flex-nowrap mb-1">
                            <span class="input-group-text" id="addon-wrapping">Status</span>
                            <input type="text" class="form-control" placeholder="Username" aria-label="Username"
                                aria-describedby="addon-wrapping" name="Size" value="<%= objDT.status %>">
                        </div>
                    </div>

                    <p>
                        <%= smg %>
                    </p>

                    <button type="submit" class="btn btn-primary text-center">Update </button>
                    <% } %>
                </div>
    </div>
</body>

</html>